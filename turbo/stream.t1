package turbo

t1 actionTemplate(action string, target string) {
	<turbo-stream action={ action } target={ target }>
		<template id={ tr.Echo("test") }>
			{ children... }
		</template>
	</turbo-stream>
}

t1 removeTemplate(action string, target string) {
	<turbo-stream action={ action } target={ target }></turbo-stream>
}
